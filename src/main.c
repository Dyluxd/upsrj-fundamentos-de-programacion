#include <stdio.h>

int main() {
        float L;

            int area;
            int volumen;
    double side, area, volume;

    printf("introduce long de la longitud del lado:");
    scanf("%lf", &side);

    area = L * L;
    volume = L * L * L;

    printf("area:%1\nvolumen:%1\n", area, volumen);

    return 0;
}
